
$(function () {
    $('#dowebok').fullpage({
        anchors: ['page1', 'page2', 'page3', 'page4', 'page5', 'page6'],
        menu: '#nav_menu',
        onLeave: function (index, nextIndex, direction) {
        }

    });
});